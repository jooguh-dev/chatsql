# 检查Submissions表数据脚本

这个脚本用于验证提交记录是否正确同步到GCP的`chatsql_system`数据库的`submissions`表中。

## 使用方法

### 方法1: 直接运行（需要激活虚拟环境）

```bash
# 激活虚拟环境
source venv/bin/activate

# 运行脚本
python check_submissions.py

# 或者指定显示记录数
python check_submissions.py --limit 20
```

### 方法2: 使用Django shell

```bash
# 激活虚拟环境
source venv/bin/activate

# 使用Django shell运行
python manage.py shell < check_submissions.py
```

## 脚本功能

1. ✅ **检查表是否存在**: 验证`submissions`表是否存在
2. 📊 **显示总记录数**: 统计表中的总记录数
3. 📋 **显示最近记录**: 显示最近的提交记录（默认10条）
4. 🔍 **验证外键关系**:
   - 验证`exercise_id`是否存在于`problems`表
   - 验证`user_id`是否存在于`users`表
5. 📈 **统计信息**:
   - 按状态（correct/incorrect）统计
   - 按练习ID统计
   - 按用户ID统计

## 输出示例

```
================================================================================
检查GCP chatsql_system数据库中的submissions表
================================================================================

✅ submissions表存在

📊 总记录数: 15

📋 最近 10 条记录:
--------------------------------------------------------------------------------

记录 #1:
  ID: 1
  查询 (query): SELECT * FROM products WHERE price > 100...
  状态 (status): correct
  执行时间 (execution_time): 0.023
  练习ID (exercise_id): 1
  用户ID (user_id): 1
  创建时间 (created_at): 2024-12-04 10:30:45.123456
  更新时间 (updated_at): 2024-12-04 10:30:45.123456
  ✅ exercise_id=1 存在于problems表
  ✅ user_id=1 存在于users表

...

📈 状态统计:
  correct: 10 条
  incorrect: 5 条

📈 按练习ID统计 (前10):
  exercise_id=1: 5 条提交
  exercise_id=2: 3 条提交
  ...
```

## 故障排除

如果遇到错误：

1. **确保虚拟环境已激活**
   ```bash
   source venv/bin/activate
   ```

2. **确保环境变量已设置**
   - 检查`.env`文件是否存在
   - 确认`GCP_DB_HOST`, `GCP_DB_USER`, `GCP_DB_PASSWORD`等变量已设置

3. **确保数据库连接正常**
   - 检查GCP Cloud SQL实例是否运行
   - 验证网络连接和防火墙设置

4. **检查依赖是否安装**
   ```bash
   pip install -r requirements.txt
   ```

